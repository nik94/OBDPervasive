<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppBaseTheme" parent="android:Theme.Material.Light.DarkActionBar">

        <!--    color for the app bar -->
        <item name="android:colorPrimary">@color/black_overlay</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/black_overlay</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/black_overlay</item>
        <item name="android:statusBarColor">@color/black_overlay</item>
        <item name="android:navigationBarColor">@color/black_overlay</item>

    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

</resources>
